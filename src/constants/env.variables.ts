export enum DatabaseConstants {
  DB_USERNAME = 'DB_USERNAME',
  DB_PASSWORD = 'DB_PASSWORD',
  DB_NAME = 'DB_NAME',
  DB_PORT = 'DB_PORT',
  DB_HOST = 'DB_HOST',
}

export enum JWTContants {
  JWT_SECRET = 'JWT_SECRET',
  JWT_EXPIRY = 'JWT_EXPIRY',
}

export enum AppConstants {
  NODE_ENV = 'NODE_ENV',
  CORS_WHITELIST = 'CORS_WHITELIST',
}
